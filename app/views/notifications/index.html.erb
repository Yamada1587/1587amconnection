<h1>Notifications#index</h1>
<p>Find me in app/views/notifications/index.html.erb</p>

<% @notifications.each do |noti| %>
 <% if noti %>
    <%= render "notifications/notifications", locals: { notification: noti } %>
 <% else %>
    <p>通知はございません</p>
 <% end %>
<% end %> 